filtering
  redis set uniqueness?
  429
  gibberish generation step on and off
  worker queue with session uuids translating into spotify/search query strings
  Proof of concept in git branch

px not %

auto play
  skip button
  rethink design

playlist

options of what data to display

harden nginx

kubernetes aws autoscaling
unit testing
