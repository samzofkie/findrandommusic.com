backend
  popularity
  real locking
  find bug in returning 0 songs

frontend
  center <Song/>s in <SongList/>
  One column not longer than the other

  nice toggle animationz
  why is toggleControlsExpanded restarting playback
  suspense
  GenreList performance
  frontend reactivity
 
  auto play order
  error handling
  auto play skip button
  selected genres float to top
  SongInfo genre remove duplicates
  duplicate songs show up?

  multirangeslider minimum value

playlist?
options of what data to display?
playback preview length?

harden nginx
readme diagrams
kubernetes aws autoscaling
